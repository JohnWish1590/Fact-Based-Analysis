# 求真分析（Fact-Based Analysis）

一个用于分析不同上市公司的可复用研究技能。输入股票代码或公司名称，即可按行业与商业模式适配分析口径，核对证据、财务质量、资本回报、估值预期和主要反证，并在条件允许时生成高可读性 HTML 报告。

## 安装

### Codex

在终端运行：

```bash
mkdir -p ~/.codex/skills
git clone https://github.com/JohnWish1590/Fact-Based-Analysis.git ~/.codex/skills/stock-truth-analysis
```

重新打开 Codex 后，技能可按相关请求自动触发，也可在提示中显式写：

```text
$stock-truth-analysis 求真分析 000792 盐湖股份
```

更新技能：

```bash
git -C ~/.codex/skills/stock-truth-analysis pull
```

### ChatGPT 或其他支持 Agent Skills 的产品

下载本仓库，将根目录作为技能文件夹导入。技能入口文件为 `SKILL.md`，界面元数据位于 `agents/openai.yaml`。不同产品的导入入口和技能目录可能不同，请按对应产品的 Skills/技能导入界面操作。

## 强制检查点

八条全流程硬约束，优先于其他步骤。每条都可被检验；做不到就在报告中说明未做到，不要静默跳过。

- **CP1 空结果验证**：任何"未检索到""没有披露""0 条命中"的结论，先做阳性对照。验证接口语义（标题检索还是全文？是否支持多词？覆盖哪些板块？），再用已知必然存在的词跑同一接口确认非空。对照失败时只能写"该工具未能验证"，不得写成"不存在"。
  - **CP1.1 公告全文检索操作**：巨潮等公告接口必须显式带 `isfulltext=true`（省略或 `false` 时只搜标题，正文关键词必然返回 0 条）；必须跑阳性对照（如 `算力` 应返回上千条）；多词组合不可靠，须降级为单词检索加人工筛选，并剔除同名公司、无关行业等无效命中；否定性结论按检索完成度分级措辞，不得越级。
- **CP2 方法级独立**：独立性按方法划分，不按任务或 agent 划分。方法类别为 ① 原始披露 ② 结构化数据接口 ③ 公司自述 ④ 第三方行业与媒体 ⑤ 实物或过程证据（招标中标、集采、拆解）。两个 agent 查同一接口＝一个来源，不得计入交叉验证。
- **CP3 主张三分法**：把产品、技术或份额主张拆成 L1 存在性、L2 商业化（流片/量产/出货/收入）、L3 竞争结果（能否赢份额）三层，分层取证。L1 的证据不得用于支撑 L3 的结论。
- **CP4 证据权重与承重清单**：按"这条证据扛着多少结论"排序复核，而不是按来源类型分级后就放行。列出 3–5 条承重主张；单一来源的承重主张须在正文显著位置说明脆弱性。对转述的参数先做量纲与算术自洽性检查。
- **CP5 先证伪**：动笔前先写 Kill Criteria 草稿，然后优先去找这些事实。证伪结果与支持证据同时呈现，不只放在报告最后一节。
- **CP6 主张台账与一次渲染**：全程维护主张台账（主张/层次/状态/来源/方法类别/置信度/复核日期），证据收敛后一次成文。禁止先发布再打补丁留下前后不一致的版本。
- **CP7 先问三个问题**：深入检索前先确认——是否有持仓需压力测试、关注的时间尺度、要的是验证观点还是公司质量或买卖时点。30 秒可避免整篇错配。
- **CP8 降级与偏差披露**：检索工具受限（限流、余额不足、付费墙、接口变更）时，在报告开头说明降级方式与它引入的样本偏差。不得把"换了一种检索方式"当作"检索更充分"。

## 技能分析什么

技能以证据、口径一致和可证伪为原则，适用于 A 股、港股和美股的不同上市公司，不依赖某一家公司的专属资料。

- **行业适配**：先判断行业、商业模式、生命周期、资本强度及收入确认方式，再选指标；不机械套用单一估值或财务模板。
- **统一财务口径**：核对报告期、币种、单位、合并范围、归母口径、一次性项目、现金与非现金项目；自行调整时列出公式、假设和来源。分部数据与整体数据冲突时，以分部数据揭示的真实驱动为准。
- **证据等级**：对关键论点区分 FACT（事实）、CALCULATION（计算）、INFERENCE（推断）和 UNCERTAINTY（不确定性）。
- **改善代价**：检查增长或利润率改善是否以降价、投入增加、资本开支、营运资金、杠杆、并购、稀释、一次性收益或风险转移为代价。
- **背离与反证**：寻找利润与现金流、订单与收入、存货与需求、管理层表述与披露之间的背离，并列出最强反方解释。
- **资本回报**：在适用时分析 ROE、ROIC 与 Incremental ROIC；说明口径与并购、周期等干扰。
- **估值与预期**：选择适合商业模式的估值方法，建立价值区间并给出上行情景与下行情景，指出哪个假设是胜负手；进行 Reverse Valuation（反推当前价格隐含的增长和回报预期）与 Expectation Gap（市场预期、兑现条件、潜在差异）。
- **持续搜索与熔断**：重要数据缺失时继续查公告、年报附注、交易所及监管披露；若关键数据仍不足以支持核心计算，就停止给出依赖它的确定结论或精确估值，并说明缺口和影响。
- **明确结论**：总结支持证据、反证、不确定性、观察指标和 Kill Criteria（推翻核心判断的条件）。需要时说明行业周期；用户要求时补充日线和周线判断。
- **自然中文与 HTML**：默认使用简体中文、连续易读的自然段；系统性研究尽可能同时提供适合桌面和手机阅读的高可读性 HTML 报告。

## 文件结构

```text
.
├── SKILL.md
├── README.md
├── CHANGELOG.md
└── agents/
    └── openai.yaml
```

## 使用示例

```text
$stock-truth-analysis 求真分析 02617.HK，核查临床数据、财务状况和估值
```

```text
$stock-truth-analysis 分析这家公司利润增长是否有现金流和资本回报支撑
```

```text
$stock-truth-analysis 从当前市值反推市场隐含预期，并列出能推翻投资逻辑的事实
```

本技能提供研究流程，不替代实时核实。分析中的市场价格和披露会随时间变化，每次使用都应重新检索并注明数据日期和来源。

## 更新日志

本技能的变更记录见 [CHANGELOG.md](CHANGELOG.md)（按时间倒序）。
