# 更新日志（Changelog）

记录本技能的所有重要变更，按时间**倒序**排列（最新在最前）。

格式：`日期 时间 — 摘要`，附提交号与文件级改动。本仓库尚未使用版本标签（tag）或 release，故以提交时间定位。

---

## 2026-09-23 22:14 — 新增八条强制检查点（CP1–CP8）

提交 `b9b0443` ｜ 2 files changed, 141 insertions(+), 12 deletions(-)

在 `SKILL.md` 中新增「强制检查点」章节，置于研究流程之前，作为全流程硬约束。八条检查点均可被检验；做不到须在报告中说明未做到，不得静默跳过。

**CP1 空结果验证**
- 任何“未检索到”“没有披露”“0 条命中”的结论，写入报告前必须先做阳性对照。
- 先验证接口语义（标题检索还是全文？是否支持多词？覆盖哪些板块？字段名与参数是否正确？），再用一个已知必然存在的词跑同一接口确认返回非空。
- 阳性对照失败时，结论只能是“该工具未能验证”，不得写成“不存在”。
- 须记录：查询词、接口、接口语义、阳性对照结果。

**CP1.1 公告全文检索的具体操作（CP1 子条款）**
- 巨潮资讯网全文检索接口必须显式带 `isfulltext=true`；**省略该参数或写成 `false` 时接口只检索公告标题**，对“51.2T”“交换芯片”这类正文关键词必然返回 0 条。这不是“没有披露”，是检索模式错了。
- 必须跑阳性对照（如 `searchkey=算力` 应返回上千条）；对照为空则接口或参数有问题，全部相关结论作废。
- 多词组合不可靠，须降级验证：改为“单词检索 + 人工筛选”，不用多词组合的 0 条结果下结论。
- 须区分“命中 0 条”与“命中但无关”，剔除同名公司、无关行业、历史财务数字等无效命中。
- 报告中须写明检索模式与对照结果（附记录格式示例）。
- 新增「否定性结论的措辞分级」表，按检索完成度分四档放开措辞，不得越级：未做对照→不得给否定性结论；仅单一关键词→“未检索到”；多词与单词交叉验证且人工筛除→“在检索范围内未发现”；穷尽上述且覆盖多方法类别→“缺乏可验证的一手支撑”。

**CP2 方法级独立**
- 独立性按**方法类别**划分，不按任务或 agent 划分。五类方法：① 原始披露（公告、年报/招股书、交易所与监管文件）② 结构化数据接口（财务、行情、股东）③ 公司自述（IR 互动平台、业绩说明会、官网产品页）④ 第三方行业与媒体 ⑤ 实物或过程证据（招标与中标文件、集采结果、产品拆解）。
- 一条结论只有在 ≥2 个不同方法类别互证时，才可称“多源确认”。
- 两个 agent 查同一接口＝**一个来源**，不得计入交叉验证。

**CP3 主张三分法**
- 把产品、技术或份额主张拆成三层分层取证：L1 存在性、L2 商业化（流片/量产/出货/确认收入）、L3 竞争结果（能否赢份额）。
- **L1 的证据不得用于支撑 L3 的结论**；跨层推理必须显式写出并标为 INFERENCE。

**CP4 证据权重与承重清单**
- 按“这条证据扛着多少结论”排序复核，而不是按来源类型分级后就放行。
- 列出 3–5 条承重主张及其来源数量与方法类别；单一来源的承重主张须在**正文显著位置**说明脆弱性，不能只藏在脚注或缺口清单里。
- 对转述的技术参数、规格与数字先做量纲与算术自洽性检查（端口数 × 速率 = 容量？同比基数是否可比？单位与币种是否一致？），自相矛盾的参数不予采信。

**CP5 先证伪**
- 研究开始前先写下 Kill Criteria 草稿，然后**优先去找这些事实**。
- 记录测试了哪些 Kill Criteria、检索了什么、结果如何；证伪结果与支持证据同时呈现，不得只放在报告最后一节。
- 用户带来一个论点时，先做的是找反例，而不是找印证。

**CP6 主张台账与一次渲染**
- 全程维护主张台账（主张 / 层次 / 状态 / 来源 / 方法类别 / 置信度 / 复核日期），证据收敛后一次成文。
- 证据未收敛时不要落盘最终报告；若因任务耗时必须先出稿，须在成稿后按台账逐条复核，并显式搜索残留旧口径（数字、评级、结论）以消除内部矛盾。
- 禁止先发布再打补丁，留下前后不一致的版本。

**CP7 先问三个问题**
- 深入检索前先确认：是否有持仓需压力测试、关注的时间尺度（季度/年度/更长）、要的是验证观点还是公司质量或买卖时点。
- 用户未说明时主动询问；无法询问时明确写出所采用的口径假设及其可能造成的偏差。

**CP8 降级与偏差披露**
- 检索工具受限（限流、余额不足、付费墙、接口变更）时，在报告**开头**说明降级方式与它引入的样本偏差。
- 不得把“换了一种检索方式”当作“检索更充分”。

**研究流程的两处改进**
- 第 6 条：分部数据与整体数据冲突时，以分部数据揭示的真实驱动为准，并解释公司口径。
- 第 9 条：估值不能只给单一反推，须给出上行情景与下行情景各自对应的价值或隐含条件，并指出哪个假设是胜负手。

**同步更新**
- `README.md` 新增「强制检查点」章节，列出 CP1–CP8 与 CP1.1 摘要，并同步上述两处流程改进。

---

## 2026-09-23 21:24 — 补充安装说明与分析范围

提交 `cede3d6` ｜ README.md +71 −1

- 新增「安装」章节：Codex 的克隆与更新命令（`~/.codex/skills/stock-truth-analysis`）；ChatGPT 及其他支持 Agent Skills 产品的导入方式（入口文件 `SKILL.md`，界面元数据 `agents/openai.yaml`）。
- 新增「技能分析什么」：列出行业适配、统一财务口径、证据等级、改善代价、背离与反证、资本回报、估值与预期、持续搜索与熔断、明确结论、自然中文与 HTML 共十项能力。
- 新增「文件结构」（`SKILL.md` / `README.md` / `agents/openai.yaml`）与「使用示例」（三个提示词示例）。
- 文末补充说明：技能提供研究流程，不替代实时核实；市场价格与披露会随时间变化，每次使用都应重新检索并注明数据日期和来源。

---

## 2026-09-23 21:23 — 新增 agents/openai.yaml

提交 `4cb8f37` ｜ agents/openai.yaml +6

- 新增界面元数据：显示名「股票求真分析」、简短描述、默认提示词（`Use $stock-truth-analysis to analyze this listed company and create an HTML report.`）。
- 设置 `policy.allow_implicit_invocation: true`，允许按相关请求自动触发。

---

## 2026-09-23 21:23 — 新增 SKILL.md

提交 `dbd1dcc` ｜ SKILL.md +39

- 新增技能入口文件 `SKILL.md`，含 YAML frontmatter（`name: stock-truth-analysis` 及英文 description，说明适用 A 股、港股、美股）。
- 「研究流程」十步：确认对象与问题、建立行业适配口径、检索并核对证据、统一财务口径、标记证据等级、分析经营改善及其代价、寻找背离与反证、评估资本回报、估值与预期、给出结论与可证伪条件。
- 定义四级证据标注：**FACT**（事实）/ **CALCULATION**（计算）/ **INFERENCE**（推断）/ **UNCERTAINTY**（不确定）。
- 含「关键缺失数据与熔断」一节（数据不足时停止给出依赖该数据的确定结论）与「输出方式」一节（简体中文自然段、证据标注、高可读性 HTML 报告）。

---

## 2026-09-23 21:16 — 初始提交

提交 `a4a94d7` ｜ README.md +1

- 仓库初始化，README 仅含标题 `# Fact-Based-Analysis`。
