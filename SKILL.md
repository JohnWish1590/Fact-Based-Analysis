---
name: stock-truth-analysis
description: Analyze a publicly listed company from its ticker or name using the user's reusable 求真分析 framework. Use for requests such as 求真分析, in-depth stock/company research, valuation review, or testing an investment thesis across A-shares, Hong Kong, and US-listed companies. Verify current filings and market data, adapt financial measures to the industry, and produce a readable Chinese analysis with a high-readability HTML report when feasible.
---

# 股票求真分析

对不同上市公司复用一套以证据、口径一致和可证伪为核心的研究流程。收到股票代码或名称后，先确认上市主体、市场、报告期和当前价格；按行业商业模式调整分析指标，不套用同一财务模板。默认用简体中文，写自然连续的分析段落，并在系统性研究中同时交付高可读性 HTML 报告。

## 强制检查点

以下八条是全流程硬约束，优先于其他章节。每条都可被检验：做不到就在报告中说明未做到，不要静默跳过。

### CP1 空结果验证

任何"未检索到""没有披露""0 条命中"的结论，写入报告前必须先做阳性对照。

- 先验证接口语义：是标题检索还是全文检索？是否支持多关键词组合？覆盖哪些市场或板块？字段名与参数是否正确？
- 再用一个**已知必然存在**的词跑同一接口，确认返回非空。
- 阳性对照失败时，结论只能是"该工具未能验证"，**不得写成"不存在"**。
- 记录：查询词、接口、接口语义、阳性对照结果。

常见陷阱：只搜标题的接口对正文词必然返回 0；多词组合可能异常返回 0；不同板块走不同后端。不做对照就会把工具缺陷误判为事实缺失。

#### CP1.1 公告全文检索的具体操作（必做）

检索上市公司公告正文时，按以下步骤执行，缺一步则结论无效：

1. **显式指定全文模式。** 巨潮资讯网全文检索接口必须带 `isfulltext=true`：

   ```text
   https://www.cninfo.com.cn/new/fulltextSearch/full?searchkey=<词>&isfulltext=true&pageNum=1
   ```

   **省略该参数或写成 `isfulltext=false` 时，接口只检索公告标题**——对"51.2T""交换芯片"这类几乎不会出现在标题里的正文关键词，必然返回 0 条。这不是"没有披露"，是检索模式错了。

2. **跑阳性对照。** 用同一接口、同一参数检索一个**已知必然存在**的词，确认返回非空。例如 `searchkey=算力` 应返回上千条。对照为空则接口或参数有问题，全部相关结论作废。

3. **多词组合必须降级验证。** 部分接口对空格分隔的多词组合会异常返回 0 条（例：`中兴通讯 算力` 返回 0，但单查 `算力` 返回 1075 条）。因此：

   - **不要**用多词组合的 0 条结果下结论。
   - 改为**单词检索 + 人工筛选**：先查核心名词（如 `51.2T`），再从返回结果中筛选目标主体。
   - 若必须用组合，先跑一次"必然有结果的组合"（如 `营业收入 增长`）验证组合语法是否被支持。

4. **区分"命中 0 条"与"命中但无关"。** 命中数不为 0 时，逐条检查命中内容是否真与目标主体相关。同一关键词可能命中同名公司、无关行业或历史财务数字。示例：检索 `51.2T` 会命中"中远航运"的月度运营数据（51.2 是运量数字），此类命中必须剔除。

5. **在报告中写明检索模式与对照结果。** 格式示例：

   > 检索：巨潮全文检索（`isfulltext=true`），查询词「盛科通信 51.2T」，命中 4 条，全部为 2023 年 IPO 文件；阳性对照「算力」命中 1075 条。4 条命中内容均为对博通、思科等竞争对手产品的行业描述，无一条描述本公司产品。

6. **否定性结论的措辞分级。** 按检索完成度选择措辞，不得越级：

   | 完成情况 | 允许的措辞 |
   |---|---|
   | 未做阳性对照 | 不得给出否定性结论 |
   | 做了对照，仅单一关键词 | "未检索到"，并注明检索词与范围 |
   | 做了对照，多词与单词交叉验证，且人工筛除无关命中 | "在检索范围内未发现"，并列出已检索范围 |
   | 穷尽上述且覆盖多方法类别（见 CP2） | 方可表述为"缺乏可验证的一手支撑" |

**本条的教训来源**：一次实际分析中，同一接口的标题检索（`isfulltext=false`）对「盛科通信 51.2T」返回 0 条，被误写为"公司从未提及该型号"；改用全文检索（`isfulltext=true`）后实为 4 条命中。另一处把"中兴通讯 51.2T"的 0 条结果当作"官方无披露"，而该公告正文确实写有"51.2T/115.2T/230.4T/460.8T 全系列框盒智算交换机"。两处错误都源于未验证接口语义。

### CP2 方法级独立

独立性按**方法**划分，不按任务或 agent 划分。

- 方法类别：① 原始披露（公告、年报/招股书、交易所与监管文件）② 结构化数据接口（财务、行情、股东）③ 公司自述（IR 互动平台、业绩说明会、官网产品页）④ 第三方行业与媒体 ⑤ 实物或过程证据（招标与中标文件、集采结果、产品拆解）。
- 一条结论只有在**≥2 个不同方法类别**互证时，才可称"多源确认"。
- 两个 agent 查同一接口＝**一个来源**，必须如实标注，不得计入交叉验证。

### CP3 主张三分法

把每个产品、技术或份额主张强制拆成三层，分层取证：

- **L1 存在性**：这个产品或能力是否被披露、是否真实存在。
- **L2 商业化**：是否流片、量产、出货、确认收入，规模多大。
- **L3 竞争结果**：是否会赢得份额、击败对手、兑现预测。

每层各自取证、各自标注证据等级。**L1 的证据不得用于支撑 L3 的结论**；跨层推理必须显式写出并标为 INFERENCE。

### CP4 证据权重与承重清单

按"这条证据扛着多少结论"排序复核，而不是按来源类型分级后就放行。

- 列出 3–5 条**承重主张**（推翻它就会改变核心结论的主张）及其来源数量与方法类别。
- 单一来源的承重主张，必须继续寻找一手或独立确认；找不到时，在**正文显著位置**说明其脆弱性，不能只藏在脚注或缺口清单里。
- 对转述的技术参数、规格与数字，先做一次**量纲与算术自洽性检查**：端口数 × 速率 = 容量？同比基数是否可比？单位与币种是否一致？自相矛盾的参数不予采信，并指出矛盾所在。

### CP5 先证伪

研究开始前，先写下 Kill Criteria 草稿（什么事实会推翻待验证的论点），然后**优先去找这些事实**。

- 记录：测试了哪些 Kill Criteria、检索了什么、结果如何。
- 证伪结果与支持证据同时呈现，不得只放在报告最后一节。
- 用户带来一个论点时，先做的是找反例，而不是找印证。

### CP6 主张台账与一次渲染

全程维护主张台账，收敛后再成文。

| 主张 | 层次 | 状态 | 来源 | 方法类别 | 置信度 | 复核日期 |
|---|---|---|---|---|---|---|
| … | L1/L2/L3 | 已证实/证伪/冲突/未找到 | 链接或文号 | ①–⑤ | 高/中/低 | … |

- 台账是唯一事实源。**证据未收敛时不要落盘最终报告**。
- 若因任务耗时必须先出稿，须在成稿后按台账逐条复核，并显式搜索残留旧口径（数字、评级、结论），消除内部矛盾。
- 禁止先发布再打补丁，留下前后不一致的版本。

### CP7 先问三个问题

深入检索前先确认，30 秒可避免整篇错配：

1. **持仓与否**——是否有仓位需要压力测试？这决定是否必须写双向论据。
2. **时间尺度**——关注季度、年度还是更长周期？这决定指标与估值口径。
3. **要什么**——是验证已有观点、评估公司质量、判断估值高低，还是判断买卖时点？

用户未说明时主动询问；无法询问时明确写出所采用的口径假设，并说明它可能造成的偏差。

### CP8 降级与偏差披露

检索工具受限（限流、余额不足、付费墙、接口变更）时：

- 在报告**开头**说明降级方式与它引入的样本偏差。例如改为抓取已知 URL，会偏向可触达来源，不是代表性样本。
- 不得把"换了一种检索方式"当作"检索更充分"。

## 研究流程

1. **确认对象与问题。** 核实公司全名、证券代码、上市地点、币种、股本口径和价格日期。若名称或代码对应多个主体，先核验后再分析。按 **CP7** 先确认用户关注的是公司质量、基本面变化、估值、交易周期，还是已有观点的验证。
2. **建立行业适配口径。** 先判断行业、商业模式、生命周期、资本强度、收入确认方式及主要驱动因素，再选择适用的经营和财务指标。解释口径选择；不要强行对所有行业使用 PE、ROE 或同一现金流框架。金融、保险、地产、生物医药、资源品、平台型和早期成长企业要采用各自合适的指标。
3. **检索并核对证据。** 优先查公司公告、定期报告、交易所披露、监管文件和原始数据；需要当前信息时搜索最新材料并注明截止日期。交叉核对关键数据及单位、币种、期间、合并范围、一次性项目、会计政策和同比基数。遵守 **CP1**（空结果先做阳性对照）与 **CP2**（独立性按方法划分）。用户明确提出缺失数据时，继续检索相关公告、附注或可信公开资料，不因一次搜索未命中就停止。
4. **统一财务口径。** 对比期间、币种和单位保持一致；区分报告值与调整值、持续经营与非持续经营、归母与合并、现金与非现金、存量与流量。所有自行调整都列出算式、假设及来源；无法可靠调整时并列呈现并解释差异，不把估算写成披露事实。
5. **标记证据等级。** 关键论点标注以下等级，不能混为一谈：
   - **FACT（事实）**：来自可核验披露或可靠原始资料，附来源和日期。
   - **CALCULATION（计算）**：基于已标注事实的可复核计算，写公式、期间和假设。
   - **INFERENCE（推断）**：由事实和计算推演的解释或判断，写出因果链及其他合理解释。
   - **UNCERTAINTY（不确定）**：资料不足、口径冲突、预测依赖强假设或无法验证之处，说明影响范围。
6. **分析经营改善及其代价。** 对收入、利润率、现金流、周转、份额或指引的改善，追问其来源与可持续性，并核查是否以降价、营销或研发投入增加、资本开支、营运资金占用、杠杆、并购、稀释、一次性收益、补贴、渠道压货或风险转移为代价。根据行业替换不适用项目；区分短期投入换取长期回报与经营质量恶化。分部数据与整体数据冲突时，以分部数据揭示的真实驱动为准，并解释公司口径。
7. **寻找背离与反证。** 按 **CP5** 在动笔前先立 Kill Criteria 并优先检索反例。主动寻找与主叙事相冲突的指标和材料，例如利润增长但经营现金流走弱、应收或存货异常、订单与收入不匹配、利用率与扩产不匹配、管理层表述与披露数据矛盾、份额提升但回报恶化。列出最强反方解释和能推翻当前判断的具体事实，避免只搜集支持论点的证据。
8. **评估资本回报。** 在适用时分解 ROE，解释杠杆、利润率与周转的贡献；计算或评估 ROIC，确保投入资本与税后经营利润口径匹配。公司处于扩张或转型期时，重点看 Incremental ROIC：新增经营利润相对于新增投入资本的回报，并说明观察期和并购、周期、一次性因素的干扰。若行业指标不适用，说明原因并使用替代指标。
9. **估值与预期。** 至少用一种适合该商业模式的估值方法建立价值区间，并呈现关键假设、情景和敏感项；不能只给单一反推，须给出上行情景与下行情景各自对应的价值或隐含条件，并指出哪个假设是胜负手。进行 **Reverse Valuation**：从当前市值/企业价值反推市场隐含的增长、利润率、回报或持续期，检查这些预期是否现实。进行 **Expectation Gap**：并列“市场可能已经定价的预期”“公司实际兑现所需条件”“尚未计价的上行与下行”，指出估值与基本面之间的差距。区分合理价值范围、市场价格及短期股价判断。
10. **给出结论与可证伪条件。** 直接回答问题，归纳支持证据、反证、关键不确定性和估值结论。按 **CP4** 标出承重主张及其来源数量。若任务涉及投资周期，说明当前周期定位及依据；如用户要求技术面，补充日线与周线结构，不用技术图形替代基本面。写出后续观察指标、验证时间点和 Kill Criteria（哪些事实会推翻核心判断）。

## 关键缺失数据与熔断

对可能改变结论、估值或风险等级的数据，主动继续搜索公司公告、年报附注、交易所文件、监管材料及可靠第三方原始来源。记录检索到的来源、报告期和口径。区分“没有披露”“暂未找到”“数据互相冲突”和“无法计算”。按 **CP1**，任何"暂未找到"都必须附阳性对照结果，否则改标为"工具未能验证"。

若关键数据在合理检索后仍缺失，或数据冲突使核心计算不可复核，执行数据不足熔断：停止给出依赖该数据的确定结论或精确估值；说明缺口、已查来源、它会改变哪些判断，并给出有条件的情景范围（仅在可辩护时）。仍可完成不依赖缺口的事实整理与局部分析。不得用编造数值、无依据的精确目标价或含糊措辞掩盖证据不足。

## 输出方式

- 默认简体中文，段落自然连贯，避免把完整分析切成过多短行或碎片化要点。标题、表格和少量关键列表用于改善阅读。
- 开头先给直接结论和判断边界；正文按经营逻辑、财务质量、资本回报、估值预期、风险与反证组织，不为凑齐模板重复无关模块。按 **CP8** 在开头披露检索工具降级及其样本偏差。
- 事实、计算、推断、不确定性需在关键论证处明确标记；给重要数字标注期间、币种、单位、来源和日期。网络来源提供可点击链接或清楚出处。按 **CP4**，单一来源的承重主张须在正文说明脆弱性。
- 系统性分析同时生成高可读性 HTML 报告，适合完整阅读和转发。报告应包含标题、分析日期、结论、分节正文、必要表格、来源链接、口径说明及不确定性；使用内嵌 CSS，保证桌面和手机可读。聊天回复提供核心结论及报告链接。若环境无法创建或提供 HTML 文件，明确说明这一部分未能完成，不要声称已生成。按 **CP6**，报告须由已收敛的主张台账一次渲染而成。
- 保持中立，不迎合用户已有仓位或观点。明确区分公司好坏、估值高低与股价走势；将看多和看空的最强论据都纳入结论。
